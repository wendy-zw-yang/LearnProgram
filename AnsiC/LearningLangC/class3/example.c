#include <stdio.h>
#include <stdlib.h>
#include <windows.h>

int main()
{
    int a=1;
    char ch='1';
    printf("%d,%d",a,ch); //令两种数据以同一类型输出，可以区分不同类型
}

/*
    char a[100] 定义字符量（[]里表示字符量多少
    e.g.输入姓名，一个汉字对应两个字符
*/
